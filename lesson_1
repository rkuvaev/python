from math import *

#Тип Int — целые числа
x = 123

#Тип Float — не целые числа
x = 12.399

#Тип Boolean
t = 2 > 4
t = False
x = True

#Тип String — строка
x = "What's up"

#Как переводить один тип данных в другой
int()
str()
float()
bool()

#Коллекции данных — объект, который хранит в себе другие объекты
# [0] [1] [2] ... [-1] [-2] ... [-n]
x = "Hello"

#Список встроенных функций

#Это функция
len(x)

#Это метод. Он не может существовать без какого-то либо объёкта. Популярные для string: find, rfind, count
print(x.find("e") + 1)
print(x.count("l"))

#Список
x = []
z = [1, 3, 4, 6]
y = ["google", "yandex", False]
y.append("rambler")

#Ввод данных. Python языком с сильной типизацией и весь input по умолчанию строки.
x = int(input("Введите число: \n"))
print(x)

#Условие if см. файл if.py

#Математические операции:

# < Меньше — условие верно, если первый операнд меньше второго.
# > Больше — условие верно, если первый операнд больше второго.
# <= Меньше или равно.
# >= Больше или равно.
# == Равенство. Условие верно, если два операнда равны.
# != Неравенство. Условие верно, если два операнда неравны.

# // Деление без остатка
# ** Возведение в степень
# % Остаток от деления
print(10 % 3)
# Опредение чётного или нечётного числа
x % 2 == 0

#Подключение математической библиотеки
x = 5
print(sqrt(x))
print(exp(x))

#Домашнее задание №1 циклы for и while
